version: '3.8'

services:
  tottem-mock-api:
    build: .
    container_name: tottem-mock-api
    ports:
      # Puerto HTTP interno (Ocean Digital maneja HTTPS automáticamente)
      - '3000:3000'
    environment:
      - PORT=3000
    restart: unless-stopped


